# Prompt Registry Assets

This directory stores versioned prompt templates and JSON schemas used by shared runtime model calls.

Each prompt is immutable after publication. Prompt template hashes are computed from canonical prompt text and schema bytes.
